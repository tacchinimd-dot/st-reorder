services:
  - type: web
    name: st-reorder
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: NODE_ENV
        value: production
